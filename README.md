# research-agent
Multi-agent RAG pipeline built with LangGraph, LangChain, and FastAPI. Decomposes research queries into sub-tasks, retrieves grounded context from FAISS vector store, augments with live web search, and synthesises comprehensive answers via GPT-4o-mini. Deployed on AWS EC2 via API Gateway with Docker containerisation.
